import { currentDirectory } from '../index';
import path from 'path';

const up = () => {
  console.log(currentDirectory);
  const newDirectory = path.resolve();
  // FILE_DICTIONARY.username =
};

export { nwd };
